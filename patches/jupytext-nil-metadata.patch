diff --git a/lua/jupytext/init.lua b/lua/jupytext/init.lua
index 1129208..2085d99 100644
--- a/lua/jupytext/init.lua
+++ b/lua/jupytext/init.lua
@@ -57,6 +57,9 @@ end
 
 local cleanup = function(ipynb_filename, delete)
   local metadata = utils.get_ipynb_metadata(ipynb_filename)
+  if metadata == nil then
+    return
+  end
 
   local _, output_extension, _ = style_and_extension(metadata)
 
@@ -68,6 +71,9 @@ end
 
 local read_from_ipynb = function(ipynb_filename)
   local metadata = utils.get_ipynb_metadata(ipynb_filename)
+  if metadata == nil then
+    return
+  end
   local ipynb_filename = vim.fn.resolve(vim.fn.expand(ipynb_filename))
 
   -- Decide output extension and style
diff --git a/lua/jupytext/utils.lua b/lua/jupytext/utils.lua
index 6019ca5..8c91774 100644
--- a/lua/jupytext/utils.lua
+++ b/lua/jupytext/utils.lua
@@ -13,7 +13,16 @@ local language_names = {
 }
 
 M.get_ipynb_metadata = function(filename)
-  local metadata = vim.json.decode(io.open(filename, "r"):read "a")["metadata"]
+  local file = io.open(filename, "r")
+  if file == nil then
+    return nil
+  end
+  local ok, decoded = pcall(vim.json.decode, file:read "a")
+  file:close()
+  if not ok or type(decoded) ~= "table" then
+    return nil
+  end
+  local metadata = decoded["metadata"]
   local language = metadata.kernelspec.language
   if language == nil then
     language = language_names[metadata.kernelspec.name]
